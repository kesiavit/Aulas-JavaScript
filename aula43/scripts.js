// Manipulando Atributos

const input = document.querySelector("input")

// input.setAttribute("disabled", true) -> consegue deixar ele desabilitado, atualizando um atributo

// input.setAttribute("type", "file") -> mudou o tipo da botão

// input.removeAttribute("id") -> retira um atributo